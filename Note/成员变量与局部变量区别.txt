成员变量与局部变量的区别：
   1：定义位置的不同
   2：作用域的不同
   3：默认值的不同
        成员变量有默认值，而局部变量没有默认值
   4：内存中的存储位置不一样
             局部变量是在栈中（跟随所在反法一起的）
             成员变量在堆中（跟随对象一起的）
   5：生命周期的不同
            成员变量时跟随对象的，在堆中存储的，由JVM来负责清理
           局部变量时跟随方法的，在栈中的，方法进栈他产生，方法出栈他死亡